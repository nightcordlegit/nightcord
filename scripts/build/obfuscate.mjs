// @ts-check
/*
 * YouCord â€” Post-build obfuscation
 * Applied to generated .js BEFORE .asar packaging and electron-builder.
 * Automatically disabled in --dev / --watch mode.
 */

import { execSync } from "child_process";
import { statSync } from "fs";
import { join } from "path";

// â”€â”€â”€ Target files â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// dist/js/   â†’ buildDesktop (YouCord app itself)
const DESKTOP_JS_FILES = [
    "dist/js/main.js",
    "dist/js/preload.js",
    "dist/js/splashPreload.js",
    "dist/js/updaterPreload.js",
    "dist/js/renderer.js",
];

// dist/desktop/ and dist/youcord/ â†’ build.mjs (vencord asar)
const ASAR_JS_FILES = [
    "patcher.js",
    "preload.js",
    "renderer.js",
];

// â”€â”€â”€ javascript-obfuscator args â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// NOTE: self-defending, rc4 encoding, split-strings and numbers-to-expressions
//       caused freezes when enabling plugins / opening settings.
//       â†’ base64 instead of rc4, reduced threshold, no split-strings.
const OBF_ARGS = [
    "--compact", "true",
    "--self-defending", "false",
    "--simplify", "true",
    "--identifier-names-generator", "hexadecimal",
    "--string-array", "true",
    "--string-array-encoding", "base64",
    "--string-array-threshold", "0.5",
    "--string-array-rotate", "true",
    "--string-array-shuffle", "true",
    "--string-array-index-shift", "true",
    "--string-array-wrappers-count", "1",
    "--string-array-wrappers-type", "variable",
    "--split-strings", "false",
    "--numbers-to-expressions", "false",
    "--unicode-escape-sequence", "false",
].join(" ");

// â”€â”€â”€ Helper â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

function obfuscateFile(filePath) {
    try {
        statSync(filePath);
    } catch {
        console.warn(`[obfuscate] Skipped (not found): ${filePath}`);
        return;
    }

    console.log(`[obfuscate] ${filePath}`);
    try {
        execSync(
            `npx javascript-obfuscator "${filePath}" --output "${filePath}" ${OBF_ARGS}`,
            { stdio: "inherit" }
        );
    } catch (e) {
        // @ts-expect-error - e is unknown in JS file
        console.error(`[obfuscate] FAILED: ${filePath}\n${e.message}`);
        process.exit(1);
    }
}

// â”€â”€â”€ Export: for buildDesktop.mts â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
/**
 * Obfuscates files in dist/js/ (generated by buildDesktop).
 */
export async function obfuscateDistJs() {
    console.log("\n[obfuscate] === Obfuscation dist/js/ ===");
    for (const file of DESKTOP_JS_FILES) {
        obfuscateFile(file);
    }
    console.log("[obfuscate] === Done dist/js/ ===\n");
}

// â”€â”€â”€ Export: for build.mjs â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
/**
 * Obfuscates .js files in a dist/ directory before createPackage() into .asar.
 * @param {string} dir  e.g. "dist/desktop" or "dist/youcord"
 */
export async function obfuscateDir(dir) {
    console.log(`\n[obfuscate] === Obfuscation ${dir}/ ===`);
    for (const filename of ASAR_JS_FILES) {
        obfuscateFile(join(dir, filename));
    }
    console.log(`[obfuscate] === Done ${dir}/ ===\n`);
}
